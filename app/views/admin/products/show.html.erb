<h2>商品詳細</h2>
<%= attachment_image_tag @product, :image ,style: "width: 320px; height: 180px;" %>
<table>
    <tr>
      <td>商品名</td>
      <td><%= @product.name %></td>
    </tr>
    <tr>
      <td>商品説明</td>
      <td><%= @product.explanation %></td>
    </tr>
    <tr>
      <td>ジャンル</td>
      <td><%= @product.genre.name %></td>
    </tr>
    <tr>
      <td>税込価格(税抜価格)</td>
      <td><%= price_tax(@product.price).to_s(:delimited) %>(<%= @product.price.to_s(:delimited) %>)円</td>
    </tr>
    <tr>
      <td>販売ステータス</td>
      <td><% if @product.sale_status == true %>販売中
          <% else %>売り切れ
          <% end %></td>
    </tr>
</table>
<%= link_to "編集する", edit_admin_product_path(@product), class: "btn btn-primary" %>